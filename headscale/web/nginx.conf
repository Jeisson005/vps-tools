server {
    listen 8080;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location /web {
        alias /usr/share/nginx/html;
        try_files $uri $uri/ /web/index.html /index.html;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
