# Local agent notes — EXAMPLE. Copy to baseline/local/agent-notes.local.md (gitignored).
# Appended verbatim to the audit prompt every run, after PROMPT.md. Use for
# personal context the repo must not contain. On conflict with PROMPT.md,
# these notes take precedence.
# Examples:
# - "Port 3159 auth was verified manually on 2026-09-04, don't re-flag it."
# - "Open-WebUI RAG embeddings are experimental; tolerate MEDIUM findings there."
# - "Maintenance window is Sundays 02:00-04:00; never suggest restarts outside it."
